<%= form_with(model: timetable, local: true) do |form| %>
  <% if timetable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(timetable.errors.count, "error") %> prohibited this timetable from being saved:</h2>

      <ul>
      <% timetable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hospital_speciality_id %>
    <%= form.text_field :hospital_speciality_id %>
  </div>

  <div class="field">
    <%= form.label :day %>
    <%= form.text_field :day %>
  </div>

  <div class="field">
    <%= form.label :start_hour %>
    <%= form.number_field :start_hour %>
  </div>

  <div class="field">
    <%= form.label :end_hour %>
    <%= form.number_field :end_hour %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
