
<div class="after_nav">
  <p class="look_hospital">Are you looking for a blood bank?</p>
  <p class="place">You are at the rigth place</p>

  <div class="search-block">
    <%= form_with(scope: :blood_bank, url: blood_banks_path, method: :get, local: true ) do |form|%>

	  <%= form.label :name, class: 'col-md-1 wite-color' %>
	  <%= form.text_field :name, class: 'col-md-1',style: "padding: 7px; width: 300px;", placeholder: 'search...' %>
	  <%= form.collection_select(:town_id, Area.all, :id, :town, class:"col-md-1", style:"width: 300px;", prompt: 'select town') %>
    <%= form.collection_select(:area_id, Area.all, :id, :name, class:"col-md-1", style:"width: 300px;", prompt: 'select area') %>
    <%= form.submit 'Search', class: 'btn btn-default'%>

 <% end %>
  </div>
  <div class="table-container">
    <table class="list-table table table-hover">
      <thead>
        <tr>
          <th>Name</th>
          <th>Area</th>
          <th>Public</th>
          <th>Googlemap link</th>
          <th>Number1</th>
          <th>Number2</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
      <% @blood_banks.each do |blood_bank| %>
          <tr>
            <td><%= blood_bank.name %></td>
            <td><%= blood_bank.area.name %></td>
            <td><%= blood_bank.googlemap_link %></td>
            <td><%= blood_bank.number1 %></td>
            <td><%= blood_bank.number2 %></td>
            <td><%= link_to 'Details', blood_bank %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
