<%= form_with(model: hospital, local: true) do |form| %>
  <% if hospital.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hospital.errors.count, "error") %> prohibited this hospital from being saved:</h2>

      <ul>
      <% hospital.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :area_id %>
    <%= form.collection_select(:area_id, Area.all, :id, :name, class:"col-md-1", style:"width: 300px;") %>
  </div>

  <div class="field">
    <%= form.label :public %>
    <%= form.check_box :public %>
  </div>

  <div class="field">
    <%= form.label :googlemap_link %>
    <%= form.text_field :googlemap_link %>
  </div>

  <div class="field">
    <%= form.label :number1 %>
    <%= form.text_field :number1 %>
  </div>

  <div class="field">
    <%= form.label :number2 %>
    <%= form.text_field :number2 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
